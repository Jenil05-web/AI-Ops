services:
  - type: web
    name: ai-ops-triage
    env: docker
    region: ohio
    plan: free
    branch: main
    healthCheckPath: /health
    envVars:
      - key: OPENAI_API_KEY
        sync: false
