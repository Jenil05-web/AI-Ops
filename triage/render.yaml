services:
  - type: web
    name: ai-ops-triage
    env: docker
    region: ohio
    plan: free
    branch: main
    rootDir: triage
    healthCheckPath: /health
    envVars:
      - key: OPENAI_API_KEY
        sync: false
